{
  "name": "family-app-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:mobile": "npm run dev --workspace @family-app/mobile",
    "dev:server": "npm run dev --workspace @family-app/server",
    "build": "npm run build --workspace @family-app/server && npm run lint --workspace @family-app/mobile",
    "backend:up": "docker compose up -d --build postgres api worker cron",
    "backend:down": "docker compose stop api worker cron",
    "backend:restart": "docker compose restart api worker cron",
    "backend:logs": "docker compose logs -f api worker cron",
    "build:mobile:android:preview": "npm run build:android:preview --workspace @family-app/mobile",
    "build:mobile:android:production": "npm run build:android:production --workspace @family-app/mobile",
    "db:generate": "npm run db:generate --workspace @family-app/server",
    "db:migrate": "npm run db:migrate --workspace @family-app/server",
    "db:seed": "npm run db:seed --workspace @family-app/server"
  }
}
